{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">Operation edit</div>
      <div class="panel-body" >
        {{ form(edit_form,{ 'style': 'horizontal','role':'form'}) }}
        <ul class="record_actions">
          <li>
              <a href="{{ path('operation') }}">Back to the list</a>
          </li>
          <li>{{ form(delete_form) }}</li>
        </ul>
      </div>
    </div>
  </div>
</div>

{% endblock %}
